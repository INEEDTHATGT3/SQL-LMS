{
  "level": 3,
  "title": "Advanced Analytics",
  "tagline": "approximate aggregates, percentile optimization, streaming, parallel",
  "placementQuiz": [
    {
      "q": "approx_count_distinct(col) vs COUNT(DISTINCT col):",
      "options": [
        "Same result, approx is faster",
        "approx is exact, COUNT DISTINCT is approximate",
        "approx only works on integers",
        "COUNT DISTINCT is deprecated"
      ],
      "answer": 0,
      "explain": "Approximate aggregates (HyperLogLog) trade small error for massive speed on large datasets."
    },
    {
      "q": "percentile_cont(0.5) WITHIN GROUP (ORDER BY salary):",
      "options": [
        "Average salary",
        "Median salary",
        "5th percentile salary",
        "50% of rows"
      ],
      "answer": 1,
      "explain": "percentile_cont(0.5) = continuous percentile at 0.5 = median."
    },
    {
      "q": "percentile_disc vs percentile_cont:",
      "options": [
        "Same",
        "disc = discrete (actual value), cont = interpolated",
        "cont = discrete, disc = continuous",
        "Only disc exists"
      ],
      "answer": 1,
      "explain": "disc = existing value at percentile; cont = interpolated between values."
    },
    {
      "q": "TABLESAMPLE with REPEATABLE ensures:",
      "options": [
        "Faster sampling",
        "Same sample every run (deterministic)",
        "Exact 10% rows",
        "No page-level bias"
      ],
      "answer": 1,
      "explain": "REPEATABLE(seed) makes sampling deterministic — same sample every run."
    },
    {
      "q": "Approximate percentile (TDigest) vs exact percentile:",
      "options": [
        "Same accuracy",
        "TDigest ~1% error, O(1) memory",
        "TDigest only for integers",
        "Exact percentile always faster"
      ],
      "answer": 1,
      "explain": "TDigest streaming algorithm: O(1) memory, ~1% error, merges across workers."
    }
  ],
  "sections": [
    {
      "title": "Approximate Aggregates — HyperLogLog, TDigest, KMV",
      "blocks": [
        {
          "type": "mantra",
          "md": "Exact COUNT(DISTINCT) = O(N) memory. Approximate = O(1) memory, ~1-2% error. HyperLogLog for distinct count, TDigest for percentiles, KMV for top-K. Essential for 1B+ row tables."
        },
        {
          "type": "code",
          "label": "hyperloglog distinct count",
          "code": "-- PostgreSQL: HyperLogLog extension\nCREATE EXTENSION IF NOT EXISTS hll;\n\n-- Create HLL sketch\nSELECT hll_add_agg(hll_empty(), user_id) FROM events;\n\n-- Distinct count from sketch\nhll_cardinality(hll_add_agg(hll_empty(), user_id));\n\n-- Merge sketches (parallel/distributed)\nhll_union(hll_sketch1, hll_sketch2);",
          "pg": "-- hll extension: hll_add_agg, hll_cardinality, hll_union",
          "mysql": "-- MySQL: APPROX_COUNT_DISTINCT() (8.0.13+)\nSELECT APPROX_COUNT_DISTINCT(user_id) FROM events;",
          "py-pandas": "# datasketches / hyperstream for Python"
        },
        {
          "type": "code",
          "label": "tdigest percentile",
          "code": "-- TDigest for approximate percentiles\nSELECT tdigest_agg(value) FROM measurements;\n\n-- Query percentiles from sketch\nSELECT percentile(t, 0.5) FROM (SELECT tdigest_agg(value) AS t FROM measurements) s;\n\n-- Merge sketches (distributed)\ntdigest_union(sketch1, sketch2);",
          "pg": "-- PostgreSQL: tdigest extension or HyperLogLog with percentile approximation",
          "mysql": "-- MySQL: no native TDigest",
          "py-pandas": "# tdigest library\nfrom tdigest import TDigest\nt = TDigest()\nfor v in data: t.add(v)\nprint(t.percentile(50))"
        },
        {
          "type": "visual",
          "kind": "table-diff",
          "spec": {
            "before": [
              {"method": "COUNT(DISTINCT)", "memory": "O(N)", "accuracy": "Exact", "speed": "Slow on large N"},
              {"method": "HyperLogLog", "memory": "O(1) ~few KB", "accuracy": "~1-2% error", "speed": "Very fast"},
              {"method": "TDigest (percentile)", "memory": "O(1) ~few KB", "accuracy": "~1% error", "speed": "Very fast"},
              {"method": "KMV (top-K)", "memory": "O(K)", "accuracy": "Exact top-K", "speed": "Fast"}
            ],
            "after": [],
            "highlight": ["memory", "accuracy"]
          }
        },
        {
          "type": "quiz",
          "items": [
            {
              "q": "HyperLogLog memory usage is:",
              "options": [
                "O(N) where N = distinct count",
                "O(log N)",
                "O(1) fixed size",
                "O(N log N)"
              ],
              "answer": 2,
              "explain": "HLL uses fixed-size registers (e.g., 2^14 registers = ~16KB). Memory constant regardless of distinct count."
            },
            {
              "q": "TDigest vs exact percentile_cont:",
              "options": [
                "TDigest always exact",
                "TDigest ~1% error, O(1) memory",
                "TDigest only for integers",
                "Cont is deprecated"
              ],
              "answer": 1,
              "explain": "TDigest approximates CDF with compressed centroids. ~1% error, constant memory."
            }
          ]
        }
      ]
    },
    {
      "title": "Streaming Analytics — Continuous Views, PipelineDB, pg_streaming",
      "blocks": [
        {
          "type": "heading",
          "text": "Continuous Aggregates (TimescaleDB)"
        },
        {
          "type": "code",
          "label": "continuous aggregate",
          "code": "-- TimescaleDB continuous aggregate\nCREATE MATERIALIZED VIEW hourly_events\nWITH (timescaledb.continuous) AS\nSELECT time_bucket('1 hour', time) AS bucket,\n  device_id,\n  COUNT(*) AS event_count,\n  AVG(value) AS avg_value\nFROM events\nGROUP BY bucket, device_id;\n\n-- Automatic refresh (background)\nSELECT add_continuous_aggregate_policy('hourly_events',\n  start_offset => INTERVAL '3 hours',\n  end_offset => INTERVAL '1 hour',\n  schedule_interval => INTERVAL '1 hour');\n\n-- Real-time view (combines materialized + raw)\nSELECT * FROM hourly_events WHERE bucket > now() - INTERVAL '1 day';",
          "pg": "-- TimescaleDB: continuous aggregates + real-time views",
          "mysql": "-- MySQL: no native continuous aggregates",
          "py-pandas": "# Scheduled incremental refresh\ndef refresh_hourly():\n    new_data = get_new_events()\n    incremental_update(hourly_agg, new_data)"
        },
        {
          "type": "heading",
          "text": "Streaming Aggregation (pg_streaming / PipelineDB)"
        },
        {
          "type": "code",
          "label": "streaming aggregate concept",
          "code": "-- Concept: aggregate as data flows in\n-- PipelineDB (PostgreSQL extension) / pg_streaming\n\n-- Continuous view\nCREATE CONTINUOUS VIEW page_views_1min AS\nSELECT site_id, COUNT(*) AS views\nFROM page_views\nGROUP BY site_id, time_bucket('1 minute', ts);\n\n-- Query continuous view (always up-to-date)\nSELECT * FROM page_views_1min WHERE site_id = 123;\n\n-- Combines: materialized (historical) + streaming (recent)",
          "pg": "-- PipelineDB / pg_streaming for streaming SQL",
          "mysql": "-- No native streaming SQL",
          "py-pandas": "# Streaming: use Faust, Bytewax, or Kafka Streams\n# Not native SQL"
        },
        {
          "type": "callout",
          "tone": "blue",
          "md": "Real-world: TimescaleDB continuous aggregates = 99% of streaming use cases. PipelineDB deprecated. For custom: Kafka + Flink/Spark Streaming + sink to DB."
        },
        {
          "type": "quiz",
          "items": [
            {
              "q": "Continuous aggregate lag (end_offset) means:",
              "options": [
                "How far behind real-time",
                "How often to refresh",
                "How much history to keep",
                "Max query latency"
              ],
              "answer": 0,
              "explain": "end_offset = how far behind real-time the aggregate is (e.g., 1 hour lag)."
            },
            {
              "q": "Continuous aggregate combines:",
              "options": [
                "Materialized view + raw tail",
                "Two materialized views",
                "View + trigger",
                "Index + partition"
              ],
              "answer": 0,
              "explain": "Real-time view = materialized (historical) + raw tail (recent data)."
            }
          ]
        }
      ]
    },
    {
      "title": "Advanced Sampling & Parallel Query",
      "blocks": [
        {
          "type": "heading",
          "text": "Parallel Query for Analytics"
        },
        {
          "type": "code",
          "label": "parallel query config",
          "code": "-- Enable parallel query\nSET max_parallel_workers_per_gather = 4;\nSET min_parallel_table_scan_size = '8MB';\nSET parallel_tuple_cost = 0.1;\nSET parallel_setup_cost = 1000;\n\n-- Force parallel for testing\nSET force_parallel_mode = on;\n\n-- Check if parallel used\nEXPLAIN ANALYZE SELECT dept_id, COUNT(*) FROM employees GROUP BY dept_id;\n-- Look for: Parallel Seq Scan, Partial Aggregate, Gather Merge",
          "pg": "-- Parallel query (PG 10+)\n-- Partial Aggregate on workers → Gather Merge at leader",
          "mysql": "-- MySQL: no native parallel GROUP BY\n-- Use partitioning + UNION ALL manually",
          "py-pandas": "# Manual parallel: split, merge in parallel\nimport dask.dataframe as dd\nddf = dd.from_pandas(df, npartitions=4)\nresult = ddf.groupby('dept_id').size().compute()"
        },
        {
          "type": "query-plan",
          "title": "Parallel Hash Join Plan",
          "plan": {
            "Plan": {
              "Node Type": "Gather Merge",
              "Workers Planned": 4,
              "Startup Cost": 10000.00,
              "Total Cost": 50000.00,
              "Plan Rows": 1000000,
              "Plan Width": 120,
              "Plans": [
                {
                  "Node Type": "Parallel Hash Join",
                  "Startup Cost": 5000.00,
                  "Total Cost": 40000.00,
                  "Plan Rows": 250000,
                  "Plan Width": 120,
                  "Hash Cond": "(a.id = b.id)",
                  "Worker 0": { "Actual Rows": 250123 },
                  "Worker 1": { "Actual Rows": 249876 },
                  "Worker 2": { "Actual Rows": 250045 },
                  "Worker 3": { "Actual Rows": 249956 }
                }
              ]
            }
          },
          "dialect": "pg"
        },
        {
          "type": "quiz",
          "items": [
            {
              "q": "Parallel Hash Join workers each build:",
              "options": [
                "Full hash table on entire build side",
                "Hash table on their partition of build side",
                "Shared hash table in shared memory",
                "No hash table — use NLJ"
              ],
              "answer": 1,
              "explain": "Each worker builds hash on its partition of build side. No shared memory — independent."
            },
            {
              "q": "Gather vs Gather Merge:",
              "options": [
                "Same thing",
                "Gather = unordered collect; Gather Merge = ordered merge",
                "Gather Merge faster",
                "Gather for Hash Join only"
              ],
              "answer": 1,
              "explain": "Gather Merge: workers return sorted data, leader merges. Gather: unordered collect."
            }
          ]
        }
      ]
    },
    {
      "title": "Dialect Mastery — Advanced Analytics",
      "blocks": [
        {
          "type": "dialect-diff",
          "sql": "SELECT approx_count_distinct(user_id) FROM events;",
          "pg": "SELECT approx_count_distinct(user_id) FROM events; -- hll extension",
          "mysql": "SELECT APPROX_COUNT_DISTINCT(user_id) FROM events; -- 8.0.13+",
          "py-pandas": "df['user_id'].nunique()  # exact"
        },
        {
          "type": "dialect-diff",
          "sql": "SELECT * FROM events TABLESAMPLE SYSTEM (10) REPEATABLE (42);",
          "pg": "SELECT * FROM events TABLESAMPLE SYSTEM (10) REPEATABLE (42);",
          "mysql": "-- No TABLESAMPLE\nSELECT * FROM events WHERE RAND() < 0.1;",
          "py-pandas": "events.sample(frac=0.1, random_state=42)"
        }
      ]
    },
    {
      "title": "Problem Set — L3 Drills",
      "blocks": [
        {
          "type": "problems",
          "items": [
            {
              "id": "X301",
              "tier": 3,
              "title": "HyperLogLog for Unique Users",
              "platform": "drill",
              "patterns": ["hyperloglog"],
              "statement": "Estimate daily active users from 100M events using HyperLogLog. Compare with exact COUNT(DISTINCT).",
              "hints": ["CREATE EXTENSION hll; hll_add_agg()"]
            },
            {
              "id": "X302",
              "tier": 3,
              "title": "TDigest for Latency Percentiles",
              "platform": "drill",
              "patterns": ["tdigest"],
              "statement": "Compute p50, p95, p99 latency from 10M requests using TDigest.",
              "hints": ["tdigest_agg(latency)", "percentile(t, 0.99)"]
            },
            {
              "id": "X303",
              "tier": 3,
              "title": "Continuous Aggregate for 5-min Buckets",
              "platform": "drill",
              "patterns": ["continuous-aggregate"],
              "statement": "Design TimescaleDB continuous aggregate for 5-min metrics with 1-hour lag.",
              "hints": ["time_bucket('5 min', ts)", "add_continuous_aggregate_policy"]
            },
            {
              "id": "X304",
              "tier": 3,
              "title": "Streaming Page Views (PipelineDB)",
              "platform": "drill",
              "patterns": ["streaming-aggregate"],
              "statement": "Create continuous view for page views per minute per site.",
              "hints": ["CREATE CONTINUOUS VIEW", "time_bucket('1 minute', ts)"]
            },
            {
              "id": "X305",
              "tier": 3,
              "title": "Parallel GROUP BY on 100M Rows",
              "platform": "drill",
              "patterns": ["parallel-aggregate"],
              "statement": "Tune parallel GROUP BY: set workers, work_mem, parallel_tuple_cost.",
              "hints": ["max_parallel_workers_per_gather", "work_mem", "parallel_tuple_cost"]
            },
            {
              "id": "X306",
              "tier": 3,
              "title": "Approximate Top-K with KMV",
              "platform": "drill",
              "patterns": ["kmv", "top-k"],
              "statement": "Find top 100 most frequent IPs from 100M requests using KMV sketch.",
              "hints": ["KMV sketch", "top-K distinct"]
            }
          ]
        }
      ]
    }
  ],
  "revisionCard": [
    "HyperLogLog: O(1) memory, ~1-2% error for COUNT(DISTINCT). hll extension.",
    "TDigest: streaming percentile algorithm, O(1) memory, ~1% error.",
    "Continuous aggregates (TimescaleDB): auto-refreshed MV with incremental refresh.",
    "Streaming aggregates: continuous view = materialized + raw tail; PipelineDB/pg_streaming.",
    "Parallel aggregate: workers do Partial Aggregate → Gather Merge combines.",
    "TABLESAMPLE SYSTEM (fast) vs BERNOULLI (accurate) + REPEATABLE for determinism."
  ],
  "glossary": [
    { "term": "HyperLogLog", "def": "Probabilistic algorithm for approximate distinct count with fixed memory." },
    { "term": "TDigest", "def": "Streaming percentile algorithm with O(1) memory and bounded error." },
    { "term": "continuous aggregate", "def": "TimescaleDB: automatically maintained materialized view with incremental refresh." },
    { "term": "continuous view", "def": "PipelineDB/pg_streaming: real-time view combining materialized history + streaming tail." },
    { "term": "parallel aggregate", "def": "Workers compute partial aggregates; leader combines via Gather Merge." },
    { "term": "KMV sketch", "def": "K Minimum Values sketch for top-K distinct elements with bounded memory." }
  ]
}